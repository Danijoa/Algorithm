#include <string>
#include <vector>

using namespace std;

int solution(string s) 
{
    int answer = 0;

    //크기 1 ~ length/2 확인
    for (int i = 1; i <= s.length(); i++)
    {
        // substr(start index, length)
        for (int j = 0; j < s.length(); j++)
        {
            if(s.substr()==s())
        }
    }
    
    return answer;
}
